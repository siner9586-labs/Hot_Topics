# ADR 003 — Explainable Heat v1

Use platform-local robust normalization and a versioned weighted additive model. Dynamic source availability affects confidence, not by injecting zeros into Heat. Store all component values in every snapshot for audit and regression testing.
