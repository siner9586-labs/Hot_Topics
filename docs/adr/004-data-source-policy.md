# ADR 004 — Source policy

Prefer official API > official feed/RSS > structured public page > explicitly permitted open interface. Never bypass authentication, CAPTCHA, paywalls, anti-bot systems or private tokens. An unavailable source remains unavailable rather than being replaced with mock production data.
