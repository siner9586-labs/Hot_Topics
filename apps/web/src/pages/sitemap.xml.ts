import { apiFetch, type ApiResult, type RankingRow } from '../lib/api';
export async function GET({locals,site}:any){const [cn,g]=await Promise.all([apiFetch<ApiResult<RankingRow[]>>(locals,'/api/v1/rankings?region=CN&limit=100'),apiFetch<ApiResult<RankingRow[]>>(locals,'/api/v1/rankings?region=GLOBAL&limit=100')]);const base=(site??new URL('https://hot-topics.example')).toString();const slugs=[...new Set([...(cn?.data??[]),...(g?.data??[])].map((x)=>x.slug))];const paths=['','global-resonance','attention-gap','history','methodology',...slugs.map((s)=>`topic/${s}`)];const xml=`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${paths.map((p)=>`<url><loc>${new URL(p,base).toString()}</loc></url>`).join('')}</urlset>`;return new Response(xml,{headers:{'content-type':'application/xml'}});}
