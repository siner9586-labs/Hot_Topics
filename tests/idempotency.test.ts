import { readFile } from 'node:fs/promises';import { describe,expect,it } from 'vitest';
describe('D1 idempotency contract',()=>{it('schema and writes contain duplicate guards',async()=>{const migration=await readFile('migrations/0001_initial.sql','utf8');const db=await readFile('packages/db/src/index.ts','utf8');expect(migration).toContain('UNIQUE(topic_id,region,captured_at)');expect(migration).toContain('UNIQUE(topic_id,source_id,captured_at)');expect(migration).toContain('PRIMARY KEY(topic_id,raw_item_id)');expect(db).toContain('INSERT OR IGNORE INTO raw_items');expect(db).toContain('INSERT OR REPLACE INTO topic_snapshots');});});
