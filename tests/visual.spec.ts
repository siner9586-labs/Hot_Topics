import { test,expect } from '@playwright/test';import { mkdirSync } from 'node:fs';
mkdirSync('artifacts/ui',{recursive:true});
test('desktop and mobile visual smoke',async({page})=>{await page.setViewportSize({width:1440,height:900});await page.goto('/');await expect(page.getByRole('heading',{name:'互联网注意力，此刻流向哪里'})).toBeVisible();await expect(page.locator('.topic-row').first()).toBeVisible();expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth)).toBe(true);await page.screenshot({path:'artifacts/ui/desktop-1440x900.png',fullPage:true});await page.setViewportSize({width:390,height:844});await page.reload();expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth)).toBe(true);await page.screenshot({path:'artifacts/ui/mobile-390x844.png',fullPage:true});await page.emulateMedia({colorScheme:'dark'});await page.reload();await page.screenshot({path:'artifacts/ui/dark-mobile-390x844.png',fullPage:true});});
